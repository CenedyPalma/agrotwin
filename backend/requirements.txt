fastapi>=0.115
uvicorn[standard]>=0.30
sqlalchemy>=2.0
pydantic>=2.7
pydantic-settings>=2.3
python-multipart>=0.0.9
Pillow>=10.3
shapely>=2.0
numpy>=1.26
opencv-python-headless>=4.9

# Optional geospatial raster support (manual import of orthomosaic/NDVI GeoTIFFs).
# Ships its own GDAL wheel on Linux/Mac; install when raster import is needed.
rasterio>=1.3

# Point-cloud tiling from OpenDroneMap LAZ output (scripts/tile_points.py)
laspy[lazrs]>=2.5
